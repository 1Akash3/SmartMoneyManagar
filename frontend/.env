# Cloudflare Turnstile SITE key (public — pairs with the secret key in backend/.env)
# Get it from: Cloudflare Dashboard → Turnstile → your widget → Site Key
# The value below is Cloudflare's official TEST key (always passes) — replace with your real site key.
VITE_TURNSTILE_SITE_KEY=0x4AAAAAADiqRl6oycR2qVLB
