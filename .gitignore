# Dependencies
node_modules/

# Build output
dist/
build/

# Environment / secrets — never commit
.env
.env.local
.env.*.local
backend/.env
frontend/.env
SMTP creds.txt

# Uploaded user files (personal financial data — never commit)
backend/uploads/*
!backend/uploads/.gitkeep

# Logs / editor / OS
*.log
.DS_Store
.vscode/

# Capacitor native projects (regenerate locally with: npx cap add android / ios)
frontend/android/
frontend/ios/
